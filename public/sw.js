// This is a placeholder file that will be replaced by the Vite PWA plugin
// The actual service worker will be generated during build time
self.addEventListener('install', () => self.skipWaiting());
self.addEventListener('activate', () => self.clients.claim());
